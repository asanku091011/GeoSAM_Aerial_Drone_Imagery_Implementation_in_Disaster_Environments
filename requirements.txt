# Disaster Navigation System - Python Dependencies
# Updated for Ultimate Dynamic Navigation with Smooth 360° Paths
# Install with: pip install -r requirements.txt

# =====================================================
# CORE NUMERICAL AND SCIENTIFIC COMPUTING
# =====================================================
numpy>=1.21.0,<2.0.0
scipy>=1.7.0,<2.0.0

# =====================================================
# COMPUTER VISION AND IMAGE PROCESSING
# =====================================================
opencv-python>=4.5.0,<5.0.0
Pillow>=8.0.0,<11.0.0

# =====================================================
# DEEP LEARNING FRAMEWORK (for Geo-SAM)
# =====================================================
torch>=1.10.0,<3.0.0
torchvision>=0.11.0,<1.0.0

# =====================================================
# DRONE CONTROL (Optional - only if using real drone)
# =====================================================
djitellopy>=2.4.0,<3.0.0

# =====================================================
# DATA VISUALIZATION (Optional but recommended)
# =====================================================
matplotlib>=3.3.0,<4.0.0

# =====================================================
# DATA HANDLING AND LOGGING
# =====================================================
pandas>=1.3.0,<3.0.0

# =====================================================
# NETWORK AND COMMUNICATION (for robot control)
# =====================================================
# paramiko>=2.7.0,<4.0.0  # For SSH/SCP (usually pre-installed)

# =====================================================
# ADDITIONAL UTILITIES
# =====================================================
# Note: Geo-SAM model weights must be downloaded separately
# Download from: https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth
# Place in: models/sam_vit_h_4b8939.pth

# =====================================================
# SYSTEM REQUIREMENTS
# =====================================================
# Python 3.8 or higher required
# Recommended: Python 3.9 or 3.10
# 
# For Raspberry Pi 5:
# - 4GB RAM minimum (8GB recommended)
# - 32GB+ microSD card
# - Python 3.9 or higher
# 
# For Windows/Linux/Mac:
# - 8GB RAM minimum
# - 10GB free disk space
# - Any modern CPU

# =====================================================
# PLATFORM-SPECIFIC NOTES
# =====================================================
# 
# Windows:
# - Install Visual C++ Build Tools if torch installation fails
# - Use 'pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu' for CPU-only
# 
# Raspberry Pi:
# - Use 'pip install torch torchvision --extra-index-url https://download.pytorch.org/whl/cpu'
# - May need to use --break-system-packages flag: pip install --break-system-packages -r requirements.txt
# 
# macOS:
# - For Apple Silicon (M1/M2), torch will use MPS acceleration automatically
# - Use 'pip install torch torchvision'

# =====================================================
# OPTIONAL DEPENDENCIES FOR DEVELOPMENT
# =====================================================
# pytest>=7.0.0  # For unit testing
# black>=22.0.0  # For code formatting
# flake8>=4.0.0  # For linting
# ipython>=8.0.0  # For interactive debugging

# =====================================================
# VERSION COMPATIBILITY NOTES
# =====================================================
# Tested with:
# - Python 3.9.13
# - numpy 1.23.5
# - opencv-python 4.7.0
# - torch 2.0.1
# - Windows 11, Ubuntu 22.04, Raspberry Pi OS (Bookworm)

# =====================================================
# INSTALLATION INSTRUCTIONS
# =====================================================
# 
# Standard installation:
#   pip install -r requirements.txt
# 
# For Raspberry Pi:
#   pip install --break-system-packages -r requirements.txt
# 
# For development (with optional packages):
#   pip install -r requirements.txt
#   pip install pytest black flake8
# 
# For CPU-only PyTorch (smaller download, faster install):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt (skip torch/torchvision lines)
# 
# Verify installation:
#   python -c "import cv2, numpy, torch; print('✓ All imports successful')"

# =====================================================
# TROUBLESHOOTING
# =====================================================
# 
# If torch fails to install:
#   1. Try CPU-only version (see above)
#   2. Check Python version (must be 3.8+)
#   3. Update pip: python -m pip install --upgrade pip
# 
# If opencv fails to install:
#   1. Try: pip install opencv-python-headless
#   2. On Raspberry Pi, may need: sudo apt-get install python3-opencv
# 
# If numpy fails with "error in numpy setup":
#   1. Update pip and setuptools: pip install --upgrade pip setuptools
#   2. Try: pip install numpy --no-cache-dir
# 
# If you see "externally-managed-environment" error on Pi:
#   1. Use virtual environment: python -m venv venv && source venv/bin/activate
#   2. Or use --break-system-packages flag (not recommended for production)
